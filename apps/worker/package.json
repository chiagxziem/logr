{
  "name": "worker",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build --compile --minify --sourcemap src/index.ts --outfile logr-worker"
  },
  "dependencies": {
    "bullmq": "^5.66.0",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@repo/db": "workspace:*",
    "@repo/redis": "workspace:*",
    "@types/bun": "latest"
  }
}
