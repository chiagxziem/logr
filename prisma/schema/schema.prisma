generator client {
  provider   = "prisma-client"
  output     = "../../generated/prisma"
  runtime    = "bun"
  engineType = "client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
