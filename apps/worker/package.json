{
  "name": "@repo/worker",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build --compile --minify --sourcemap src/index.ts --outfile worker-exe",
    "start": "./worker-exe"
  },
  "dependencies": {
    "@t3-oss/env-core": "^0.13.10",
    "bullmq": "^5.66.4",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@repo/db": "workspace:*",
    "@repo/redis": "workspace:*",
    "@types/bun": "^1.3.5"
  }
}
